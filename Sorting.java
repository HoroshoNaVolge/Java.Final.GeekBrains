import java.util.Comparator;
import java.util.HashSet;
import java.util.stream.Stream;

/*public class Sorting {

    public static void SortSet(HashSet<Notebook> set) {
       

        set.stream(new NotebookComparator())
                .forEach(p -> System.out.printf("%s (%s) - %d \n",
                        p.getName(), p.get(), p.getPrice()));
    }

    class NotebookComparator implements Comparator<Notebook>{


        @Override
        public int compare(Notebook o1, Notebook o2) {

            o1.getName().toUpperCase().compareTo(o2.getName().toUpperCase());
        }
}*/
